import React from 'react';

import PerfilForm from './perfil_form'
const Perfil = (props) => {
   return (
         <PerfilForm
         history={props.history}
         />
   )
}

export default Perfil;